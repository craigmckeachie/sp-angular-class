#!/bin/bash

ORIGINAL_PATH=$PATH
DIR=$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd)
export COURSE_HOME=${DIR%/*}

PATH=$DIR:$COURSE_HOME/node_modules:$PATH

chmod u+x $COURSE_HOME/node_modules/.bin/*
